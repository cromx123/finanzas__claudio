# Mejoras pendientes

Lista de cosas para mejorar o añadir, basada en revisión del código actual (no son ideas al aire — cada ítem indica dónde está el gap).

## Bugs / funcionalidad incompleta

- [x] ~~**"Dividendos cobrados" está hardcodeado en 0**~~ — resuelto: el backend ahora reconstruye la cantidad de acciones tenida en cada `ex_date` desde el historial de transacciones (`_dividends_collected` en [services/api/app/modules/portfolios/service.py](services/api/app/modules/portfolios/service.py)) y expone `dividendos_cobrados_bruto`/`_neto` en `PortfolioSummaryOut`, consumido en [apps/web/app/panel/page.tsx](apps/web/app/panel/page.tsx). Test de regresión: `test_dividends_collected_uses_share_count_held_on_ex_date`.
- [x] ~~**No se puede editar una transacción**~~ — resuelto: edición inline en [TransactionHistoryModal.tsx](apps/web/components/panel/TransactionHistoryModal.tsx) (lápiz por fila → fecha/cantidad/precio editables → check/x para guardar/cancelar). Backend: `PATCH /v1/portfolios/{id}/transactions/{tx_id}` (`update_transaction` en [portfolios/service.py](services/api/app/modules/portfolios/service.py)) — activo y tipo compra/venta quedan fijos (eso sí sigue siendo borrar-y-recargar), pero corrige fecha/cantidad/precio sin perder el registro. Revalida que una venta editada no exceda lo disponible (excluyéndose a sí misma del cálculo). Tests: `test_update_transaction_fixes_a_typo`, `test_update_transaction_cannot_oversell`.
- [x] **Portafolios limitados a CLP/USD/EUR** — [apps/web/lib/types.ts](apps/web/lib/types.ts) (`Currency`). El Screener ya soporta acciones del Nikkei 225 (JPY, agregado esta sesión) pero nunca se pueden añadir portafolios en JPY, podriamos añadir esta divisa y asi poder crear un portafolio para acciones .t .
- **Costo siempre a precio promedio** — [services/api/app/modules/portfolios/service.py](services/api/app/modules/portfolios/service.py) (`run_ledger`, `avg_cost`). No hay selección de lote (FIFO/LIFO/lote específico) al vender, lo que importa para cálculo de impuestos.
- **Sin importación de transacciones** — existe exportación a CSV de movimientos ([lib/export/movements.ts](apps/web/lib/export/movements.ts)) pero no el camino inverso (cargar transacciones masivas desde un CSV/broker).
- **Objetivos (módulo Goals)** — no lo revisé a fondo esta sesión; vale la pena una pasada para ver si tiene los mismos gaps que Panel/Perfil tenían antes de esta ronda de cambios.

## Funcionalidades nuevas sugeridas

- [x] ~~**Toggle de modo oscuro**~~ — resuelto: nuevo [context/Theme.tsx](apps/web/context/Theme.tsx) persiste la preferencia en localStorage y aplica/quita `.dark` en `<html>`; un script inline en [app/layout.tsx](apps/web/app/layout.tsx) la aplica antes de la hidratación (sin parpadeo del tema claro); botón sol/luna en [NavBar.tsx](apps/web/components/layout/NavBar.tsx), visible en todas las páginas. Verificado con Playwright: el tema cambia por completo y persiste al recargar.
- [x] ~~**Alertas de precio**~~ — resuelto: nueva pestaña "Alertas" en Perfil ([AlertsPanel.tsx](apps/web/components/perfil/AlertsPanel.tsx)), seguimiento de cualquier ticker (esté o no en un portafolio). Backend: tabla `alerts` (migración `0002_alerts`), módulo [modules/alerts](services/api/app/modules/alerts/), chequeadas por el worker EOD existente (`check_price_alerts` en [ingestion/scheduler.py](services/api/app/modules/ingestion/scheduler.py), corre tras cada cierre de mercado). Solo in-app — sin email/push, no hay proveedor configurado. Alertas de dividendos próximos ya existían (UpcomingDividends en Panel) y quedan fuera de esta pestaña.
- **Historial real de tipo de cambio para Perfil** — hoy la conversión a moneda única usa el FX rate *actual*, no el vigente en cada fecha histórica. Afecta la precisión si se agrega una vista de patrimonio combinado en el tiempo.
- **Selección de lote al vender** (ligado al punto de costo promedio arriba).

## Infraestructura / deuda técnica

- **Redis está en el stack pero no se usa** — declarado en [services/api/app/core/config.py](services/api/app/core/config.py) y en `docker-compose.yml`, pero no hay ninguna llamada real a caché/cola en el código. O se usa (cachear `/v1/screener`, el fetch de `^GSPC` del gráfico de evolución, etc. — varios endpoints hacen llamadas en vivo a Yahoo que podrían cachearse) o se saca del stack.
- **Sin caché en endpoints que pegan a Yahoo en vivo** — `POST /v1/screener` (agregar ticker), `GET /v1/assets/{symbol}/price-on-date` (fallback), y el benchmark de `GET /v1/portfolios/{id}/performance` no cachean nada más allá del `staleTime` de React Query en el frontend. Ver conversación sobre "cargar más rápido" — la recomendación de pre-ingestar tickers ya se aplicó parcialmente (`add_transaction` ahora hace ingesta completa la primera vez), pero un caché de verdad (Redis) sería el siguiente paso.
- **Flujo de build de `apps/web` en Docker** — durante esta sesión hubo confusión real sobre si el contenedor `web` reflejaba el código más reciente (no bastaba con `docker compose build` sin forzar). Vale la pena revisar el Dockerfile/cache de `apps/web` para que un rebuild normal siempre recoja los cambios sin necesitar `--no-cache`.
